export default async function postList ({data}) {
    {
        data.data.map((data.data, index ) => (
            return(
                <p key={index}>{data.data}</p>
            ) 
      ))
    }   
}